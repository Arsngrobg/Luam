#> luam:stdlib/mulf($R(0), $R(1))
#  @param $R(0) a word, the accumulator
#  @param $R(1) a word, the operand
#   |- Executes IEEE-754 floating-point multiplication between $R(0) and $R(1)
#   |- The result is stored in $R(0)
